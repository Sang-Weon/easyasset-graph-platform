{
  "name": "easyasset-graph-platform",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "test": "turbo test",
    "firebase:emulators": "firebase emulators:start",
    "neo4j:seed": "cd neo4j-setup && npx tsx seed/generate-test-data.ts"
  },
  "devDependencies": {
    "turbo": "^2.3.0",
    "typescript": "^5.7.0"
  }
}
